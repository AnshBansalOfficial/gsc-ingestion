<?xml version="1.0" encoding="UTF-8"?>
<!--
  Tests log to the console only. The CloudWatch appender is deliberately absent so that
  `mvn test` needs no AWS credentials — the AI agent runs the suite in a bare clone.
-->
<configuration>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} %-5level %logger{28} - %msg%n</pattern>
    </encoder>
  </appender>
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
  </root>
</configuration>
